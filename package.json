{
  "name": "punch",
  "version": "1.0.0",
  "private": true,
  "description": "Punch — a self-contained timesheet SPA that saves to a Markdown file",
  "type": "module",
  "scripts": {
    "test": "node --test \"tests/**/*.test.js\"",
    "serve": "node scripts/serve.mjs",
    "icons": "node scripts/make-icons.mjs"
  },
  "engines": {
    "node": ">=20"
  }
}
